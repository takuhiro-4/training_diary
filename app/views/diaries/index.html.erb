<p id="notice"><%= notice %></p>

<h1>ボディメイク記録</h1>

<%= month_calendar diaries: @diaries do |date, diaries| %>
  <%= date.day %>

  <% diaries.each do |diary| %>
    <div>
      <%= diary.name %>
    </div>
  <% end %>
<% end %>

<%# <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @diaries.each do |diary| %>
      <%# <tr>
        <td><%= diary.name %></td>
        <%# <td><%= diary.start_time %></td>
        <%# <td><%= link_to 'Show', diary %></td>
        <%# <td><%= link_to 'Edit', edit_diary_path(diary) %></td>
        <%# <td><%= link_to 'Destroy', diary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <%# </tr> %>
    <%# <% end %>
  <%# </tbody> %>
<%# </table> %>

<br>

<%= link_to '新しい 日記', new_diary_path %>
